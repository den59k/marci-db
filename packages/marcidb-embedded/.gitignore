/node_modules

# Build output (bunup) — produced from src/*.ts by `bun run build` / prepublishOnly.
/dist

# Prebuilt native libraries are produced by build-lib.sh / CI and bundled at publish time.
/native

# Generated test client fixtures (codegen output from helpers.ts / binary-bench.ts)
/test/.gen

# Packed tarballs from `npm pack`
*.tgz
