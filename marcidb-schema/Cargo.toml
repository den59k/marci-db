[package]
name = "marcidb-schema"
version.workspace = true
edition = "2024"

[dependencies]
# Foundation crate — pure schema model + parser + snapshot codec + migration authoring. No storage,
# and (deliberately) no dependency on the engine; the engine depends on this crate.
