# MarciDB JS / TS client generator

## How to compile binary

Linux:

```
cargo build -p marcidb-ts  --release --target x86_64-unknown-linux-gnu
```

Windows:
```
cargo build -p marcidb-ts --release --target x86_64-pc-windows-msvc
```